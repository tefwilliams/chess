[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "chess"
version = "0.0.1"
requires-python = ">= 3.10"
dependencies = [
    'pygame',
]
readme = "README.md"

[project.urls]
repository = "https://github.com/tefwilliams/chess"